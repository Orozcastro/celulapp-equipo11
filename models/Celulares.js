class Celular {
  constructor(id, marca, modelo, sistemaOperativo, ram, almacenamiento, color) {
    this.id = id;
    this.marca = marca,
    this.modelo = modelo,
    this.sistemaOperativo = sistemaOperativo,
    this.ram = ram,
    this.almacenamiento = almacenamiento,
    this.color = color
  }
}

module.exports = Celular;